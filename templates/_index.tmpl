{{define "content"}}
<div>
  <h2>Patrick Dewey</h2>
  <section>{{.HTMLContent}}</section>
  <section>
    <ul
      class="flex flex-col mt-1 mb-0 space-x-0 space-y-2 md:flex-row md:space-x-5 md:space-y-0 list-none pl-0"
    >
      {{template "social-link" (linkItem "https://github.com/ptdewey" "github")}} 
      {{template "social-link" (linkItem "https://bsky.app/profile/pdewey.com" "bluesky")}} 
      {{template "social-link" (linkItem "/rss.xml" "rss")}}
    </ul>
  </section>
</div>
{{end}} 
{{template "base" .}}
